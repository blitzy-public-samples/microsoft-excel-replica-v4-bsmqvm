# This file serves as a template for environment variables required by the Microsoft Excel backend API.
# Copy this file to create your own .env file and fill in the appropriate values for your environment.

# Node environment (development, staging, production)
NODE_ENV=development

# Server port
PORT=3000

# Database connection string
DATABASE_URL=postgresql://username:password@localhost:5432/excel_db

# JWT secret for authentication
JWT_SECRET=your_jwt_secret_here

# API key for external services
API_KEY=your_api_key_here

# CORS origin (comma-separated list of allowed origins)
CORS_ORIGIN=http://localhost:3000,https://excel.example.com

# Redis URL for caching and session management
REDIS_URL=redis://localhost:6379

# Log level (error, warn, info, debug)
LOG_LEVEL=info

# Azure AD configuration for enterprise authentication
AZURE_AD_TENANT_ID=your_azure_ad_tenant_id
AZURE_AD_CLIENT_ID=your_azure_ad_client_id
AZURE_AD_CLIENT_SECRET=your_azure_ad_client_secret

# IMPORTANT NOTES:
# 1. Never commit the actual .env file containing real values to version control.
# 2. Ensure that the .env file is added to .gitignore to prevent accidental commits.
# 3. Different environments (development, staging, production) may require different values.
# 4. Use strong, unique values for secrets and keys in production environments.
# 5. Regularly rotate secrets and API keys for enhanced security.
# 6. Consider using a secrets management service for production deployments.